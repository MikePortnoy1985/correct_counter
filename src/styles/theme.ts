import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
	palette: {
		primary: {
			main: '#101E5A',
		},
		background: {
			paper: '#5D80B6',
			default: '#acbdd3',
		},
	},
})

export default theme
